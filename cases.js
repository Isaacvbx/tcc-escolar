// cases.js - atualmente apenas placeholder; poderia buscar dados dinamicamente via fetch
document.addEventListener('DOMContentLoaded', () => {
  // aqui você pode implementar fetch() para carregar cases de uma API
  // por enquanto, o cases.html já contém todos os links/DOIs estáticos inseridos no HTML
  console.log('cases.js carregado — sem ações dinâmicas (por enquanto).');
});
